[project]
name = "team-agents-poc"
version = "0.1.0"
description = "Learning project for Claude Code agents — future integration with claude-maestro"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "claude-agent-sdk>=0.1.36",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
